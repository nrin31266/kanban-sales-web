export interface SubProductResponse {
    id: string
    size: string
    color: string
    price: number
    quantity: number
    images: string[]
    productId: string
    discount?: number
    createdAt: string
    updatedAt: string
  }